@yield('openForm')

<div class="page-head">
    <div class="d-md-flex justify-content-md-between">
        <div class="item">
            <h3>@yield('title')</h3>
        </div>
        <div class="item">
            @yield('buttons')
        </div>
    </div>
</div>
